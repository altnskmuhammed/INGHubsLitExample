{
  "name": "inghubslitexample",
  "version": "1.0.0",
    "type": "module",
    "scripts": {
        "start": "web-dev-server --open --watch --node-resolve",
        "test": "web-test-runner 'test/**/*.test.js' --playwright --coverage",
        "build": "rollup -c",
        "lint": "eslint 'src/**'"
    },
    "devDependencies": {
        "@open-wc/testing": "^4.0.0",
        "@web/dev-server": "^0.4.6",
        "@web/test-runner": "^0.20.2",
        "web-test-runner": "^0.0.36"
    },
    "dependencies": {
        "@vaadin/router": "^2.0.0",
        "lit": "^3.3.1"
    }
}
